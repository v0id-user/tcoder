# Minimal fly.toml for ephemeral FFmpeg batch jobs
# No http_service - machines are created on-demand via Machines API
# Billing only occurs during job execution

app = 'fly-tcoder-ffmpeg-worker-31657fa'
primary_region = 'fra'

[build]
  dockerfile = "Dockerfile"
