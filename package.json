{
	"name": "tcoder",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"fly"
	],
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types",
		"fly:build": "cd fly && bun run build:docker",
		"fly:deploy": "cd fly && bun run deploy",
		"fly:docker:build": "cd fly && bun run docker:build",
		"fly:docker:push": "cd fly && bun run docker:push",
		"fly:docker:deploy": "cd fly && bun run docker:deploy"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.56.0"
	},
	"dependencies": {
		"effect": "^3.19.13",
		"hono": "^4.11.1"
	}
}
